# Deploy Bicep infrastructure at resource group scope.
# Assumes the resource group already exists.

$RG_NAME = "rg-b3-secure-api-dev-we-01"

az deployment group create `
  --resource-group $RG_NAME `
  --parameters infra/dev.bicepparam `
  --name "b3-e1-infra-skeleton" `
  --output table